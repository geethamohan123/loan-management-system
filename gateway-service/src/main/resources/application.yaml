server:
  port: 8000
spring:
  application:
    name: gateway-service
  cloud:
    gateway:
      routes:
        - id: auth-service
          uri: http://localhost:9090/api
          predicates:
            - Path=/auth/**

        - id: customer-service
          uri: http://localhost:9091
          predicates:
            - path=/customer/**